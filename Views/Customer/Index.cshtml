@using CarWorkshopManagementSystem.DTOs // Zmieniono na DTOs
@model List<CustomerDto> // ZMIENIONO: Model to lista CustomerDto

<h2>Lista klientów</h2>

<p>
    <a asp-controller="Customer" asp-action="Create" class="btn btn-primary">Dodaj klienta</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Imię i nazwisko</th>
            <th>Telefon</th>
            <th>Liczba pojazdów</th> @* DODANO: Kolumna dla liczby pojazdów *@
            <th>Akcje</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var customer in Model)
        {
            <tr>
                <td>@customer.FullName</td>
                <td>@customer.PhoneNumber</td>
                <td>@customer.NumberOfVehicles</td> @* DODANO: Wyświetlanie liczby pojazdów *@
                <td>
                    <a asp-controller="Customer" asp-action="Details" asp-route-id="@customer.Id" class="btn btn-sm btn-info">Szczegóły</a> |
                    <a asp-controller="Customer" asp-action="Edit" asp-route-id="@customer.Id" class="btn btn-sm btn-warning">Edytuj</a> |
                    <a asp-controller="Customer" asp-action="Delete" asp-route-id="@customer.Id" class="btn btn-sm btn-danger">Usuń</a>
                </td>
            </tr>
        }
    </tbody>
</table>